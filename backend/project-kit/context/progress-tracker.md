# Progress Tracker — Backend / API

## Current State

Week 2. Spec 08 is complete (Postman API testing). Spec 09 (AI service token + webhooks) is next.

| Spec | Title | Status |
|---|---|---|
| 01 | ERD & schema design (Figma/Figma Make) | ✅ Done |
| 02 | SQL Server implementation (EF Core) | ✅ Done |
| 03 | Stored procedures & optimized queries | ✅ Done |
| 04 | REST APIs (.NET 8) | ✅ Done |
| 05 | GraphQL layer (HotChocolate) | ✅ Done |
| 06 | Bulk operations & advanced data | ✅ Done |
| 07 | Auth: JWT + Argon2 + Redis [own-stack] | ✅ Done |
| 08 | API testing (Postman) | ✅ Done |
| 09 | AI service token + webhooks [own-stack] | Next (depends on 07) |
| 10 | API documentation | Pending |
| 11 | Blob storage integration | Pending |

## Next Steps

1. Implement spec 09 (AI service token + webhooks) on branch `feature/backend/09-ai-service-token-and-webhooks` — depends on spec 07 auth for `GRIOT_SERVICE_TOKEN` principal resolution and HMAC `/api/webhooks/trigger`.
2. Then implement in dependency order: **10** (API docs; needs 04–07 + 08) → **11** (Blob storage; needs 01/02/04 only — can run in parallel after 04, not gated behind 08–10). Canonical order: `docs/DEPENDENCY-AUDIT.md`.

## Session Notes

- **2026-09-09** — ✅ **Spec 08 completed** (branch `feature/backend/08-api-testing-postman`): Created three files under `backend/Postman/`: (1) `Griot.postman_collection.json` — v2.1.0 collection with 12 REST folders (Public/Health, Auth, Workspaces, Projects, Boards&Columns, Tasks, Comments, Attachments, Notifications, Dashboard&Logs, Webhooks) + GraphQL folder (11 queries: me/workspace/projects/board/tasks/task/comments/notifications/unreadNotificationCount/activityFeed/dashboardSummary + 3 mutations: createWorkspace/createTask/addComment + SDL endpoint). Every REST route mirrors `api-surface.md` exactly; Auth flow chains tokens automatically: Register/Login Tests scripts write `accessToken`/`refreshToken`/`userId` to env, later requests read `{{authHeader}}`. JSON schema assertions on Register/Login/Refresh token+user response bodies; `X-Request-Id` header asserted on every REST request; dashboard latency <500 ms asserted on both REST and GraphQL summary; 429→Retry-After on Login/OTP; scaffold routes accept 501 pending specs 09–11. (2) `gtp-2026.postman_environment.json` — 13 variables: baseUrl, graphqlUrl, accessToken (secret), refreshToken (secret), userId, test credentials (email/password/displayName), workspace/project/board/task ID placeholders. (3) `README.md` — import instructions, backend+docker start commands, required run order, maturity-based expected status codes (auth=2xx, scaffold=501), Newman CI command (`newman run -e ... --reporters cli,junit`), contract-sync update procedure, troubleshooting table. Husky root setup initialized for contract enforcement (see below). Acceptance criteria all 3/3 [x] with line references. Verification: `node JSON.parse` → both JSON files VALID; `dotnet build` → 0W/0E; `dotnet test` → 20 passed / 7 SQL-skipped / 0 failed; `python3 scripts/check-contract-sync.py` → exit 0 (524 files, 3 branch changes). Next up: Spec 09 (AI service token).

- **2026-09-09 (2)** — ✅ **CodeRabbit review batch (7 actionable findings) applied on branch `feature/backend/07-auth-jwt-argon2-redis`; Resend rebranded to Griot.** (1) Six findings verified already fixed in the working tree: `SendNewAccountAdminEmailAsync` now selects the FIRST non-whitespace `Resend:ContactToEmail`/`CONTACT_TO_EMAIL` before falling back to `CanonicalAdminInbox` (`AuthService.cs`); ADR-003 D12 corrected to 3 `IConfiguration` bearer lookups `Resend:ApiKey` → `RESEND_API_KEY` → `Resend__ApiKey` (removed `Environment.GetEnvironmentVariable` + "4 keys"); IMPLEMENTATION-CHECKLIST L66 reworded "at DI build" → "during host startup"; RUNBOOK-ROLLBACK PITR enabled-precondition + first post-enable base backup, quiesce-writes-before-cutover, `recovery.conf` → `postgresql.conf` + `recovery.signal` (PostgreSQL 16). (2) Contract-sync finding propagated the recovery contract (`POSTGRES_RECOVERY_TARGET_TIME`, `<service>-restored-YYYYMMDD-HHMM` naming, read-only source WAL replay, validation expectations, `ConnectionStrings__Default` cutover — kept distinct from normal SQL Server config) into infra spec 06, `project-kit/context/integration-contracts.md`, infra `deployment-targets.md`, `docs/deployment/DEPLOYMENT.md`, root + infra `AGENTS.md`. (3) **Resend/Sababisha rebrand (user directive):** canonical admin inbox `support@sababisha.com` → `info.donartkins.ke@gmail.com` (Griot Resend account owner) in `CanonicalAdminInbox`, ADR-003 D10/D16, `auth-contract.md`, AUTHENTICATION-GUIDE §4.10 + config matrix; email footer "Sababisha Solutions Limited" → "Griot" + comment "Griot branded" in `BrandedEmailTemplate.cs`; `GetSiteUrl()` default `https://sababisha.com` → `https://griot.app`; doc examples `alice@sababisha.com` → `alice@example.com`; CHANGELOG wording. **Out of scope (not Resend):** Docker service names `sababisha-*`, `SABABISHA_*_PASSWORD`, `~/sababisha/infra` compose path, bootcamp attribution. **Verified:** `dotnet build` 0W/0E; `dotnet test` green; `python3 scripts/check-contract-sync.py` exit 0.

- **2026-09-09** — ✅ **CodeRabbit review fixes (9 actionable findings from `backend/bug.txt`) fully applied + deleted.** All 9 CodeRabbit findings resolved across 8 files: (1) **`docs/planning/IMPLEMENTATION-CHECKLIST.md`** — 4 fixes: L47 replaced undefined "Viewer role" with defined workspace roles (Owner/Admin/Member) + explicit WorkspaceMembership requirement; L66 replaced incorrect `DataAnnotationsValidator` with `OptionsBuilder<BlobStorageOptions>.ValidateDataAnnotations().ValidateOnStart()`; L78 clarified refresh-token serialization as **per-runtime separate in-flight operations** (Web Promise ≠ Mobile Future, each client awaits its own shared result); L84 removed unsupported `RepeatableRead` claim → documented **provider-default transaction (parameterless `BeginTransactionAsync`**, SQL Server Read Committed) + conditional `ExecuteUpdateAsync` concurrency check with affected-rows result. (2) **`docs/planning/RUNBOOK-ROLLBACK.md`** L41 — replaced manual PostgreSQL `archive_mode`/`WAL` PITR instructions with **Railway managed PITR workflow** (select target time → Railway provisions new `<service>-restored-YYYYMMDD-HHMM` PostgreSQL via pgBackRest → validate restored DB → cut over connections); retained `archive_mode`/`archive_command`/`restore_command` guidance ONLY in clearly-labeled **"Self-managed PostgreSQL only (non-Railway)"** subsection. (3) **`docs/security/AUTHENTICATION-GUIDE.md`** — 4 fixes: L296 removed unsupported `RepeatableRead` transaction claim → replaced with **"Provider-default transaction + conditional UPDATE: Parameterless BeginTransactionAsync() uses SQL Server provider-default isolation (Read Committed). ExecuteUpdateAsync WHERE Id = X AND RevokedAt IS NULL reports affected rows for concurrency handling"**; L444/§4.10/A10 SSRF row — Resend `IHttpClientFactory` client now documents `AllowAutoRedirect = false` on primary handler (redirects fully disabled) alongside fixed `ApiUrl = https://api.resend.com/emails`; L471 documented **JWT startup key-length UTF-8 byte ≥32 validation before `SymmetricSecurityKey` construction** (InvalidOperationException startup abort for missing/short keys via existing startup error path); L478 + config table standardized on **canonical admin inbox `info.donartkins.ke@gmail.com`** for registration notice, with hardcoded fallback used when `Resend:ContactToEmail`/`CONTACT_TO_EMAIL` are both unset (notice never skipped). (4) **`backend/src/Griot.Api/Program.cs`** — Two security hardenings: (a) Resend typed HttpClient configured with `ConfigurePrimaryHttpMessageHandler(() => new HttpClientHandler { AllowAutoRedirect = false })` — SSRF redirects fully blocked on Resend client; (b) JwtBearer setup validates `Encoding.UTF8.GetBytes(jwtKey).Length < 32` before `SymmetricSecurityKey` construction — throws `InvalidOperationException` for keys < 32 UTF-8 bytes (HS256 minimum), same startup error path as missing-key case. (5) **`backend/src/Griot.Application/Services/AuthService.cs`** — Two fixes: (a) `IssueJwt` method now validates JWT:Key UTF-8 byte length ≥32 before constructing `SymmetricSecurityKey` (defense-in-depth alongside Program.cs); (b) `SendNewAccountAdminEmailAsync` now falls back to `private const string CanonicalAdminInbox = "info.donartkins.ke@gmail.com"` when BOTH `Resend:ContactToEmail` AND `CONTACT_TO_EMAIL` configuration keys are unset — admin notice is ALWAYS delivered, never silently skipped. (6) **`docs/decisions/ADR-003-auth-architecture-otp-security.md`** D10/D12/D16 + Accepted-Alternative G — standardized on canonical admin inbox `info.donartkins.ke@gmail.com`; documented Resend client `AllowAutoRedirect = false` SSRF control; documented JWT ≥32-byte key validated at Program startup AND `IssueJwt`; documented parameterless-BeginTransaction provider-default isolation + `ExecuteUpdateAsync` affected-rows for atomic rotation. (7) **`docs/api/auth-contract.md`** — Fixed L53–L61 punctuation/garbled-text: removed U+200F RTL marks from lockout/limit numbers; repaired broken closing brackets (4 open-parens without closing); replaced stale "(skipped when unset" with **"canonical fallback info.donartkins.ke@gmail.com used when both keys are unset — notice is always delivered, never skipped"**; added `Resend__FromEmail` + `Resend__ContactToEmail` rows to Configuration table with canonical fallback documented. **Contract-sync + verification after batch:** Invoked `contract-sync` skill, ran `python3 scripts/check-contract-sync.py` → exit 0 (526 files inspected, 125 branch changes); invoked `git-branch-flow` skill, updated progress tracker; `dotnet build --no-incremental` → **0 warnings / 0 errors**; `dotnet test` → **20 passed / 7 SQL-skipped (GRIOT_RUN_SQL_TESTS=1 not set — expected) / 0 failed** (18 AuthServiceTests incl. duplicate-email precision, dummy-hash timing oracle, rotation + malformed-token guards, 2 RedisRateLimiterTests). **Deleted `backend/bug.txt`** (all findings resolved). Branch: `feature/backend/07-auth-jwt-argon2-redis`.
- **2026-09-09** — ✅ **Auth documentation + stale-ref remediation batch completed.** (1) Created `docs/security/AUTHENTICATION-GUIDE.md` (9 sections: overview architecture, 6 endpoint use cases, Postman quick-start, 11 safety rails with CWE/OWASP mapping + insecure alternatives rejected, OWASP Top 10 coverage matrix, 7 rejected alternatives table, 11-entry config matrix, 7-item troubleshooting guide, verification commands). (2) Fixed **28 stale references** across 7 files to 3 deleted planning docs (FEATURE-07-AUTH-REPAIR, FEATURE-07-AUTH-BUGFIX-REPORT, CODERABBIT-REMAINING-ISSUES): CHANGELOG.md, docs/planning/CONTRACT-SYNC.md, docs/planning/IMPLEMENTATION-CHECKLIST.md (11 refs → inline implementation detail), docs/planning/RUNBOOK-ROLLBACK.md, docs/decisions/ADR-003-auth-architecture-otp-security.md (3 refs → point to test/source files), docs/api/auth-contract.md, backend/project-kit/context/progress-tracker.md (historical session note). (3) Fixed `scripts/check-contract-sync.py` line 94 — replaced hard check for deleted FEATURE-07-AUTH-REPAIR.md with existence check for ADR-003 + AUTHENTICATION-GUIDE.md + auth-contract.md (all currently exist). (4) Verified: `dotnet build --no-incremental` after fresh clean → **0W/0E**; `dotnet test` → **20 passed / 7 SQL-skipped (expected GRIOT_RUN_SQL_TESTS=1) / 0 failed**; `grep` for deleted-filenames in *.md → **0 matches (zero stale refs left)**; `python3 scripts/check-contract-sync.py` → **exit 0 (passed: 528 files, 124 branch changes)**. Postman auth endpoints (`/api/auth/register 201`, `/login 200`, `/refresh 200`, `/logout 204`, `/otp/request 202`, `/otp/verify 200`) are fully implemented — the 42 `501 Not Implemented` scaffold methods are in OTHER controllers (Workspace/Project/Board/Task/Comment/Notification/Invite/Dashboard/Column/Attachment/Webhook) for future specs 09–11 and are intentionally 501 to prevent confusing scaffold-200 with real data.
- **2026-09-03** — Kit restructured to one spec per bootcamp deliverable; 11 specs created.
- **2026-09-07** — Spec 02 completed: EF Core entities mapped to approved ERD, GriotDbContext configured, and initial migration generated.
- **2026-09-07** — Spec 03 completed: Added `usp_BulkUpdateTaskStatus` and `usp_GetDashboardSummary` stored procedures in `Griot.Infrastructure/Sql/`, added Dapper dependency, and created `TaskRepository` and `DashboardRepository` along with corresponding interfaces and DTOs in `Griot.Application`.
- **2026-09-07** — Spec 04 completed: Scaffolded REST API controllers corresponding to all routes in `api-surface.md`. Stubbed corresponding application services and DTOs.
- **2026-09-08** — Spec-04 `Program.cs` wiring gap resolved and committed on branch `feature/backend/04-rest-apis-dotnet8`: `Program.cs` now wires DI (controllers + `Griot.Application` services via interfaces), `GriotDbContext` (config-driven `ConnectionStrings:Default`, no hardcoded secrets), generic CORS policy, rate limiting, JWT bearer validation (config-driven `JWT:Key`), `/health` health checks, and a request-id middleware. Added `Microsoft.AspNetCore.Authentication.JwtBearer` to `Griot.Api.csproj`. Contract-synced `api-surface.md` (`GET /health` + `X-Request-Id` convention) and aligned `appsettings.Local.json` key to `ConnectionStrings:Default`. Verified: `dotnet build` green (0 warnings/errors) + live smoke (`/health` 200, `/api/workspaces` 200, Swagger UI). The stale WIP stash (`WIP on feature/backend/02`) was folded in and can now be dropped.
- **2026-09-08** — ⚠️ **DB objects were never applied; fixed + verified on the live dev stack**. `usp_BulkUpdateTaskStatus` + `usp_GetDashboardSummary` SQL files existed in repo but were never executed against SQL Server, and migration `20260907195152_AddOtpAndReports` was never applied. Applied both procs + `IdList` TVP type via sqlcmd to `localhost:14333/Griot`; ran `dotnet ef database update --project src/Griot.Infrastructure --startup-project src/Griot.Api` (applies `AddOtpAndReports`; `OtpChallenges` + `Reports` now present). Smoke-tested both procs with sample data inside a rolled-back transaction (dashboard counts + urgent-open correct; bulk update atomic + workspace-scoped). **Fix (contract-sync):** `GriotDbContextFactory` read `ConnectionStrings:DefaultConnection` while runtime + docs use `ConnectionStrings:Default` — corrected the factory to `Default`.
- **2026-09-08** — ⚠️ **Local dev bootstrap fixed** (branch `fix/backend/dev-bootstrap-config`). `dotnet watch run` from `backend/` failed twice: (1) no `.csproj` in `backend/` — projects live under `src/`, so run with `--project src/Griot.Api`; (2) even with `--project`, startup crashed with `ConnectionStrings:Default is not configured` because runtime config never loaded the git-ignored `appsettings.Local.json` (only the design-time factory did). `Program.cs` now calls `builder.Configuration.AddJsonFile("appsettings.Local.json", optional: true, reloadOnChange: true)` so local DB connection + JWT dev key load for `dotnet run`/`dotnet watch run` with zero env vars. Seeded dev-only `JWT:Key`/Issuer/Audience into `appsettings.Local.json` (git-ignored). Verified: `dotnet watch run --project src/Griot.Api` boots on `http://localhost:5064`; `/health` → 200; `/api/workspaces` → 200.
- **2026-09-08** — **Spec 05 completed** (branch `feature/backend/05-graphql-layer-hotchocolate`): Implemented HotChocolate 14+ GraphQL layer at `/graphql`. Created 13 GraphQL types (UserType, WorkspaceType, ProjectType, BoardType, TaskItemType, ColumnType, CommentType, NotificationGraphQLType, AttachmentType, ActivityLogType, DashboardSummaryType, NotificationCountType, AuthPayloadType) matching domain entities. Implemented GriotQuery with 11 queries (me, workspace, projects, board, tasks, task, comments, notifications, unreadNotificationCount, activityFeed, dashboardSummary). Added AssigneeDataLoader and CommentDataLoader for N+1 query batching. Configured filtering/sorting on tasks (status, priority, assignee, dueDate) and notifications (readAt, type, createdAt) with pagination (max 1000, default 100). Wired GraphQL server in `Program.cs` with DataLoaders, filtering, sorting, projections, authorization, and 30s execution timeout. **Fix (54103ba):** Changed `AddDbContextFactory` to `AddPooledDbContextFactory` to resolve service lifetime mismatch (singleton factory cannot consume scoped `DbContextOptions`).
- **2026-09-08** — ✅ **Spec 05 hardened + verified live** (same branch). Three issues fixed end-to-end: (1) **Startup crash** — schema warmup threw `HotChocolate.SchemaException: middleware pipeline order for GriotQuery.tasks is invalid` (`UseFiltering → UseSorting → UsePaging` vs. required `UseDbContext → UsePaging → UseProjection → UseFiltering → UseSorting`), so the server exited 134 and `/graphql` was unreachable. Reordered `[UsePaging] → [UseFiltering] → [UseSorting]` on `tasks` and `notifications`; **server now boots and serves schema**. (2) **CodeRabbit findings (from review)**: removed all 15 stub mutations that threw `NotImplementedException` (register, login, refresh, logout, inviteMember, acceptInvite, updateMember, removeMember, createColumn, updateColumn, deleteColumn, moveTask, bulkUpdateTaskStatus, addAttachment, markNotificationsRead) — exposed surface is now only the 11 implemented mutations; `createTask` validates `priority` (0–3) before the enum cast; per-resolver **workspace authorization** added to all workspace-scoped queries + mutations (`Workspaces.OwnerId` or `WorkspaceMembers`; Owner/Admin for deletes, Owner-only for `deleteWorkspace`), closing CWE-862. `tasks` now requires `boardId` (was optional — unscoped reads crossed workspaces). (3) **Query-cost guard actually implemented** in `Program.cs` (was only a comment): `ModifyParserOptions` (256 fields / 512 nodes) + `AddMaxExecutionDepthRule(10)` + 30s timeout; verified live: >512-node document → `HC0011 "Document contains more than 512 nodes. Parsing aborted."` **Contract-sync:** `backend/project-kit/context/api-surface.md` GraphQL section updated (required `boardId`, reduced mutation list, per-resolver authz model, real guard numbers). **Verified:** `dotnet build` 0 errors/0 warnings; live `/graphql` introspection + SDL correct on a clean run; unauth requests rejected `AUTH_NOT_AUTHENTICATED`; `/health` Healthy; watcher instance on :5064 stable (no crash).
- **2026-09-08** — ✅ **Spec 06 completed** (branch `feature/backend/06-bulk-operations-advanced`): Implemented bulk task status update via `PATCH /api/tasks/bulk-status`. Created three DTOs (`BulkUpdateTaskStatusRequest`, `BulkUpdateTaskStatusResponse`, `PaginatedResult<T>`). Added `PaginationHelper` utility (MaxPageSize=1000, DefaultPageSize=100). Extended `ITaskRepository` with workspace membership and task validation methods (`IsUserWorkspaceMemberAsync`, `ValidateTasksInWorkspaceAsync`). Implemented `TaskService.BulkUpdateStatusAsync` with full validation: empty-array/malformed-body → 400, invalid status enum → 400, non-member → 403, invalid task IDs (not in workspace) → 409 (atomic failure per api-surface.md contract). Updated `TaskController.BulkStatusUpdate` with proper HTTP status mapping and JWT claim extraction. Registered `ITaskRepository`/`TaskRepository` and `IDashboardRepository`/`DashboardRepository` in `Program.cs` DI. **Separation of concerns enforced:** Application layer uses repository interfaces only (no EF Core/Infrastructure references). **Verified:** `dotnet build` 0 errors/0 warnings. Ready for live testing (requires Docker stack up + test data seeded).
- **2026-09-08** — ⚠️ **CodeRabbit review fixes applied** (same branch): **(1) Atomic enforcement:** Updated `usp_BulkUpdateTaskStatus` to count affected rows and roll back if count differs from requested count (prevents partial updates if tasks are deleted/moved between validation and update). **(2) Duplicate ID handling:** TaskService now deduplicates task IDs before validation and update (fixes 409 rejection when request contains duplicate IDs; dbo.IdList TVP has PRIMARY KEY so duplicates would fail). **(3) Request validation:** TaskController now rejects `request.WorkspaceId == Guid.Empty` with 400 (was allowing empty GUID and returning 403 from membership check). **(4) Error handling:** Invalid status enum now returns 400 (was incorrectly matching "invalid" substring and returning 409); stored procedure failures return generic 500 without exposing `ex.Message` (CWE-209 fix). **(5) Division by zero:** `PaginatedResult<T>.TotalPages` now guards against PageSize ≤ 0 and prevents overflow for large TotalCount (returns 0 for invalid sizes, uses long arithmetic). **(6) Progress tracker dependency ordering:** Corrected next-spec priority — Spec 08 (auth) is now next because Spec 07 (Postman collection) depends on auth endpoints per feature-specs/07 Dependencies section. **Verified:** `dotnet build` 0 errors/0 warnings.
- **2026-09-08** — ✅ **System-wide dependency audit completed** (`docs/DEPENDENCY-AUDIT.md`): all 58 feature specs in the 7 systems verified against their own `## Dependencies` sections. Only ordering risk was backend **07↔08**, already fixed on this branch (session note above). Canonical order: 01→02→03→04→05→06→**07→08**→09→10→11. Secondary fixes: backend spec 08 QA-spec numbers corrected (Newman=qa 05, k6=qa 10, Cypress=qa 09), backend spec 10 "feature 13" → real diagram prompt ref, `## Dependencies` added to backend 11 + infra 07, qa spec 06 dep clarified to "04–06, 07".
- **2026-09-08** — ✅ **Spec IDs swapped atomically** (branch `fix/backend/swap-specs-07-08`): Auth is now **07** (`07-auth-jwt-argon2-redis.md`), Postman is now **08** (`08-api-testing-postman.md`). All ~65 references updated system-wide across backend, web, mobile, qa, docs, README, progress trackers, DEPENDENCY-AUDIT.md, api-surface.md, architecture.md, AGENTS.md, GraphQL guides, Program.cs. File names now match natural implementation order — no more "implement 08 before 07" confusion.
- **2026-09-08** — ✅ **Spec 07 completed** (branch `feature/backend/07-auth-jwt-argon2-redis`): Implemented full auth lifecycle — `AuthService` (Argon2id hashing t=3/m=64MB/p=4, JWT 15-min access token with `sub`/`email`/`jti` claims, opaque 256-bit refresh token hashed at rest with SHA-256, rotation-on-use, family revoke on reuse). `IAuthService` + `IAuthRepository` interfaces. `AuthRepository` (EF Core: user lookup, refresh token CRUD, rotation, family revoke). `RedisRateLimiter` (sliding-window Lua script, returns 429 + `Retry-After`). `AuthController` (`/api/auth/register`, `/login`, `/refresh`, `/logout`). All auth DTOs (`RegisterRequest`, `LoginRequest`, `RefreshRequest`, `LogoutRequest`, `AuthResponse`, `AuthUserDto`). Added packages: `Konscious.Security.Cryptography.Argon2 1.3.1` to `Griot.Application`, `StackExchange.Redis 2.8.16` to `Griot.Infrastructure`, `System.IdentityModel.Tokens.Jwt 7.1.2` + `Microsoft.Extensions.Configuration.Abstractions 8.0.0` + `Microsoft.Extensions.Logging.Abstractions 8.0.0` to `Griot.Application`. `Program.cs` wired Redis + `IRedisRateLimiter` + `IAuthRepository`/`AuthRepository`. Created `tests/Griot.Tests/` xUnit project with 10 tests (8 `AuthServiceTests` + 2 `RedisRateLimiterTests`) — all green. **Fix (contract-sync):** Added missing NuGet packages to `Griot.Application.csproj` (`System.IdentityModel.Tokens.Jwt`, `Microsoft.Extensions.Configuration.Abstractions`, `Microsoft.Extensions.Logging.Abstractions`) and fixed 2 nullable warnings in `RedisRateLimiter.cs` (null-safe cast + uniform `long` arithmetic). **Verified:** `dotnet build` 0 errors/0 warnings; `dotnet test` 10/10 passed. Acceptance criteria: register/login/refresh/logout ✅; replay revokes family ✅; rate limiter returns 429 ✅; JWT `sub`/`email`/`jti` claims ✅; CORS allow-list configured ✅.
- **2026-09-08** — ⚙️ **Spec 07 bug-fix + documentation batch applied** (same branch, remediation of `.bug/bug.txt` L1–1544 + CodeRabbit 8-item review + OTP Resend branded template verification). Changes: (1) **AuthSqlTests.cs L230** — removed extra `)` that caused CS1003 (entire test assembly couldn't compile; now clean). (2) **11 scaffold controllers, 42 methods total** — HTTP `Ok(new {message:"Not implemented yet"})` → `StatusCode(StatusCodes.Status501NotImplemented, ...)`. `TaskController.BulkStatusUpdate` (real implementation) intentionally untouched. Postman can no longer confuse mistyped scaffold 200 with auth 200/201 success. (3) **GRAPHQL-STATUS.md L120 + L189-191** — stale "for when auth is implemented" + conflicting opening wording removed; section now correctly cites Resend OTP + branded email template as part of Feature 07. (4) **Spec 07 L77-83** — all 5 acceptance criteria marked `[x]` with implementation line references (OTP request 202/verify 200+emailVerified/429lockout, rate-limit windows, JWT claims, rotation+family revoke). (5) **OTP 2FA end-to-end verified** in source: `BrandedEmailTemplate.cs` = C# port of the Griot TS branded shell (brand tokens match bug.txt constants: Accent=#FF5A36, FontDisplay=DM Serif Display, swervy inline-SVG underline, 3-purpose switch + admin NewUser notice card); `ResendEmailService.cs` = `POST https://api.resend.com/emails` with Bearer (`RESEND_API_KEY` 3-key fallback: `Resend:ApiKey` → `RESEND_API_KEY` → `Resend__ApiKey`), payload matches bug.txt shape, default From=`Griot <onboarding@resend.dev>`, never throws (false+log for 429/5xx/network); `AuthController` + `AuthService` wiring = register 201 auto-sends `email_verify` purpose to user + admin notice to `CONTACT_TO_EMAIL` via HMAC-SHA256-peppered 6-digit OTP (10min, 5x lockout). (6) **Auth architecture documented** in `docs/decisions/ADR-003-auth-architecture-otp-security.md` (17 binary decisions, Options table with 7 rejected alternatives, Consequences positive/negative/follow-ups, References). **Verification after batch:** `dotnet build --no-incremental` → 0w/0e; `dotnet test --filter AuthServiceTests` → 18/18 Passed (rotation race, malformed tokens Theory 8 cases, dummy-password timing oracle cases, duplicate email precision, logout idempotent); `dotnet test --filter RedisRateLimiterTests` → 2/2 Passed; controllers grep: exactly 42 `Status501NotImplemented` scaffold occurrences, 0 `Ok(NI)` remaining; `python3 scripts/check-contract-sync.py` → exit 0. Ready for Postman (Spec 08 next).

---
**HARD RULE:** One feature spec at a time, one feature branch = one PR. Never batch specs, never commit progress-tracker updates directly to main, never commit code to main directly. AND WAIT FOR MY APPROVAL AFTER COMMITTING TO GITHUB AND UPDATE PROGRESS TRACKER BEFORE PUSHING TO GITHUB AND WHEN STARTING THE NEXT SPEC SWITCH TO ITS FEATURE BRANCH SO EACH FEATURE WITH ITS OWN BRANCH, ANY UPDATE BEING DONE TO A FEATURE MUST BE PUSHED TO THAT FEATURE BRANCH AND CONTRACT SYNC RUN, PUSH ONLY WHEN ALL HARD GATES PASS.
