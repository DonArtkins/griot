# Project Kit — Backend / API System (Griot)

The backend system (Week 2 of the bootcamp): .NET 8 + ASP.NET Core Web API + EF Core 8 + Dapper + HotChocolate + SQL Server 2022 + PostgreSQL 16 + Redis.

```
backend/project-kit/
├── README.md
├── context/
│   ├── architecture.md    ← solution layout, DI, middleware, auth
│   ├── data-layer.md      ← entities, EF Core, Dapper, procs, indexes
│   ├── api-surface.md     ← REST routes + GraphQL types (contract for all clients)
│   ├── code-standards.md  ← C#/.NET/T-SQL rules
│   └── progress-tracker.md
├── feature-specs/         ← 9 specs, one per bootcamp deliverable (01–09)
├── diagrams/              ← backend-relevant Figma Make/Figma artifacts
└── examples/              ← reference artifacts
```

## Reading order

Root `AGENTS.md` → `backend/AGENTS.md` → `research/week-02-*.md` → the approved ERD → these context files → the current spec.

**Engineering Excellence. Production Mindset. Professional Impact. 🚀**
