# Week 06 — Quality Engineering Prompts

**Week goal (from the roadmap):** QE fundamentals → manual testing → API testing (Postman/Newman) → unit & integration (xUnit, Jest+RTL, Flutter) → automated UI (Cypress) → non-functional (k6, OWASP, a11y) → TDD with ≥80% coverage.

## Status

Not started — write the prompts when Week 5 deploys exist. Draft on the agent prompt below:

> Build **Feature 10** (`feature-specs/10-quality-engineering.md`). Add the xUnit suites (incl. refresh-rotation replay + bulk-update atomicity), Jest+RTL component tests, Flutter widget/integration tests, Cypress core-loop E2E (MSW-stubbed copilot), Newman contract runs in CI, k6 load scripts, the OWASP review doc, and coverage enforcement ≥80% on the service layer + auth (not a flat aggregate pass).